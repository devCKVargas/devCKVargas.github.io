Day 30 of #100DaysOfCode

📖 Today`s discoveries

• SASS: CSS Preprocessor:
  ➥ @mixin and @include
  ➥ % placeholder selector and @extend
• Box-model: Fill area
• How units are converted from relative to absolute (pixels)
• Properties related to text are inherited (ff, fs, fw, color, etc.)
• Maintainable & scalable code: 7-1 Pattern
• Basic Responsive Design Principles
• npm outdated / update
• cli basic commands such as:
  ➥ mkdir & echo > file.ext (create folder & file)
  ➥ rmdir & del (delete folder & file)
• ...more!

//?===== more!
• Rely more on specificity than on the order of selectors
  ➥ But, rely on order when using 3rd-party stylesheets - put your stylesheet last
• pseudo-class counts as 1 class in specificity
• How units are converted from relative to absolute (pixels)
  ➥ % based calculations uses the parent`s width as the reference
  ➥ em(lengths) uses current element`s font-size as the reference
• SASS (Syntactically Awesome Style Sheets)
  ➥ darken(), lighten() functions
• cli basic commands such as: 
  ➥ dir(list files & folders in directory)
  ➥ mkdir(create folder)
  ➥ echo > file1.txt echo > file2.txt (creates multiple files)
  ➥ rmdir /s /q (delete folder) 
    ➥ (s = all files & subdirectories)
    ➥ (q = quiet mode, no confirmation prompts)
  ➥ del(delete files)

Basic Responsive Design Principles
1. Fluid Grids & Layouts
  ➥ To allow content to easily adapt to the current viewport width used to browse
the website. Uses % rather than px for all layout-related lengths.
2. Flexible / Responsive images
  ➥ Images behave differently than text content, and so we need to ensure that
they also adapt nicely to the current viewport.
3. Media Queries
  ➥ To change styles on certain viewport widths (breakpoints), allowing us to
create different version of our website for different widths.

Layout Types
1. Float
2. Flexbox
3. CSS Grid

✍ I love how I learned the basics of SASS, and I will be able to use CSS preprocessor on my previous & future projects 